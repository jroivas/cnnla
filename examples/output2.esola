68 -> A

# Basic connetion, A always fields to `stdout`
A --> stdout

# Force space now
' ' -!> stdout

# Numeric presentation as signed
A --> stdout

' ' -!> stdout

# Character representation of lowest 8 bits
A -!> stdout

' ' -!> stdout

# Stderr
A -!> stderr

'\n' -!> stdout
